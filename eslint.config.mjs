import nextCoreWebVitals from "eslint-config-next/core-web-vitals";

const config = [
  // Next.js 16 ships first-class ESLint flat config packages.
  // This replaces the old FlatCompat-based setup.
  ...nextCoreWebVitals,
];

export default config;
